<h1> All Articles </h1>

<ul id = "articles" >
  <% @articles.each do |article| %>
    <li>
    <strong> <%= link_to article.title, article_path(article), class: 'article_title'  %> </strong>
  </li>
  <% end %>
</ul>

<br>
<br>
<%= link_to "Create New Article", new_article_path, id: "new-article-link" %>
